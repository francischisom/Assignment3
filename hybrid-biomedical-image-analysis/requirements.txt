# Hybrid Biomedical Image Analysis - Python dependencies
# Google Colab already ships most of these; install locally with:
#   pip install -r requirements.txt
# The Ollama server + models are installed separately (see README):
#   ollama pull qwen2.5vl:7b
#   ollama pull llama3.2:3b

# Core scientific stack
numpy
pandas
matplotlib
pillow

# Image processing (Otsu thresholding, morphology, regionprops)
scikit-image

# Deep learning (PyTorch U-Net)
torch
torchvision

# Local LLM client (Qwen2.5-VL + Llama 3.2 via Ollama)
ollama

# Notebook runtime (not needed if you only run in Google Colab)
jupyter
